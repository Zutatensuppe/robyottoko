#!/bin/sh -ex

rm build/* -rf

# server build
npm run rollup -- -c rollup.server.config.js
cp -r src/templates build/server/

# frontend build
npm run vite -- build

# move required files for running the bot
mkdir build/scripts
cp scripts/bot build/scripts/bot
cp run build/run
